
   class CallModel {
          final String name;
          final String date;
          final String callType;
          final String profileImage;

          CallModel(this.name, this.date, this.callType, this.profileImage);
        }
